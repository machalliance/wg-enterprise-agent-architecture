import { disableTool } from "eve/tools";

/**
 * See `bash.ts`.
 *
 * Reference data has to be versioned and citable or a Tier A repair cannot be
 * justified. A tool that fetches an arbitrary URL is neither, and a citation
 * pointing at the open web is not a citation a reviewer can re-check six months
 * later.
 */
export default disableTool();
