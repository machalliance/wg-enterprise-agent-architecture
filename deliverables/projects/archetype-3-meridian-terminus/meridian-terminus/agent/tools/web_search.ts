import { disableTool } from "eve/tools";

/**
 * See `web_fetch.ts`. Also: searching the web for a beneficiary's name is a
 * different activity from screening one, and an agent that can do the first
 * will eventually be described as having done the second.
 */
export default disableTool();
