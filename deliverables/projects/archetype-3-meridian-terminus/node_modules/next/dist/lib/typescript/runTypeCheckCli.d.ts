import type { TypeCheckResult } from './runTypeCheck';
export declare function runTypeCheckCli({ baseDir, tsConfigPath, tscPath, cacheDir, }: {
    baseDir: string;
    tsConfigPath: string;
    tscPath: string;
    cacheDir?: string;
}): Promise<TypeCheckResult>;
